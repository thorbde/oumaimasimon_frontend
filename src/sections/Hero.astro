<div id="hero">
  <h1>
    Oumaima
    <div class="ampersand">&</div>
    Simon
  </h1>
  <div class="dateAndButton">
    <p class="date">11 Juli 2026</p>
    <div class="rsvpButtonContainer">
      <a class="rsvpButton" href="/rsvp">Anmälan</a>
      <p>Anmäl dig senast <strong>28 februari 2026</strong></p>
    </div>
  </div>
</div>

<style>
  #hero {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 4rem 1.5rem;
    text-align: center;
    min-height: calc(100vh - var(--header-height));
  }

  h1 {
    font-family: var(--font-display);
    font-weight: 400;
    font-style: normal;
    font-size: 3.5rem;
    line-height: 1;
    text-align: center;
    color: var(--color-primary-900);

    .ampersand {
      font-size: 1.5rem;
      position: relative;
      top: 0.2em;
    }
  }

  .dateAndButton {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 6rem;
  }

  .rsvpButtonContainer {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }

  .rsvpButton {
    background-color: var(--color-primary-500);
    font-family: var(--font-display);
    text-transform: uppercase;
    color: var(--color-bg);
    padding: 0.85rem 1.75rem;
    border-radius: 4px;
    border: none;
    cursor: pointer;
    box-shadow: 0 0.5rem 0.7rem rgba(0, 0, 0, 0.3);
    transition:
      transform 0.15s ease,
      box-shadow 0.15s ease;
  }

  .rsvpButton:hover {
    transform: translateY(-1px);
  }

  .rsvpButton:active {
    transform: translateY(0);
    box-shadow: none;
  }

  @media (min-width: 768px) {
    .rsvpButton {
      font-size: 2rem;
      padding: 1rem 1.75rem;
    }

    h1 {
      font-size: 7rem;

      .ampersand {
        font-size: 2.5rem;
      }
    }
  }
</style>
