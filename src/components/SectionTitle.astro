---
const { title, description } = Astro.props;
---

<div>
  <h2>{title}</h2>
  <p>{description}</p>
</div>

<style>
  div {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  h2 {
    font-family: var(--font-display);
    font-weight: 100;
    line-height: 1.5;
    font-size: 3rem;
  }
</style>
